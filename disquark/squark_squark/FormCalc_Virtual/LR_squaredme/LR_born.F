* LR_born.F
* form factors for {MU, MU} -> {MSf[1, 3, 1], MSf[2, 3, 1]}
* generated by FormCalc 6.1D0 24 Jun 2011 11:46

	subroutine LR_born
	implicit none

#include "LR_vars.h"


        Ctree(5) = Ctree(5) + (4*Pi)/3.D0*(Alfas*F3)/(-MGl2 + T) + 
     -   4*Pi*(Alfas*F4)/(-MGl2 + U)

! 	if(debug_virtual) then
! 	  write(*,*) 'in virtual_born: Ctree(5) =', Ctree(5)
! 	endif

        Ctree(6) = Ctree(6) - 4*Pi*(Alfas*F3)/(-MGl2 + T) - 
     -   (4*Pi)/3.D0*(Alfas*F4)/(-MGl2 + U)

! 	if(debug_virtual) then
! 	 write(*,*) 'in virtual_born: Ctree(6) =', Ctree(6)
! 	endif

	end
